#
# /opt/bro/share/bro/peers/peers.bro
#
# Generated by Chef for <%= node[:fqdn] %>
# 

type NodeLogExtension: record {
	node: string &log;
};

function add_log_node_extension(path: string): NodeLogExtension
        {
        return NodeLogExtension($node=peer_description);
        }

redef Log::default_ext_prefix = "_";
redef Log::default_ext_func = add_log_node_extension;
