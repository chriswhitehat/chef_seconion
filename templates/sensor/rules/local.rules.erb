#
# /etc/nsm/rules/<%= @sniff[:sensorname] %>/local.rules
#
# Generated by Chef for <%= node[:fqdn] %>
#

##########################
# Global
##########################

<% @sigs[:global_sigs].each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Regional
##########################

<% @sigs[:regional_sigs].each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor Group 
# <%= node[:seconion][:sensor][:sensor_group] %>
##########################

<% @sigs[:sensor_group_sigs].each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>

##########################
# Host
##########################

<% @sigs[:host_sigs].each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>


##########################
# Sensor 
##########################

<% @sigs[:sensor_sigs].each do |msg, sig| %>
	<% if sig %>
<%= "\# #{msg}" %>
<%= "#{sig}" %>

	<% end %>
<% end %>
