#
# /etc/nsm/<%= @sniff[:sensorname] %>/snort_override.conf
#
# Generated by Chef for <%= node[:fqdn] %>
#

##########################
# Global
##########################

<% @global_vars.each do |var, enabled| %>
	<% if enabled %>
<%= "#{var}" %>
	<% else %>
<%= "\##{var}" %>
	<% end %>
<% end %>

##########################
# Regional
##########################

<% @regional_vars.each do |var, enabled| %>
	<% if enabled %>
<%= "#{var}" %>
	<% else %>
<%= "\##{var}" %>
	<% end %>
<% end %>

##########################
# Sensor Group 
# <%= node[:seconion][:sensor][:sensor_group] %>
##########################

<% @sensor_group_vars.each do |var, enabled| %>
	<% if enabled %>
<%= "#{var}" %>
	<% else %>
<%= "\##{var}" %>
	<% end %>
<% end %>

##########################
# Host
##########################

<% @host_vars.each do |var, enabled| %>
	<% if enabled %>
<%= "#{var}" %>
	<% else %>
<%= "\##{var}" %>
	<% end %>
<% end %>

##########################
# Sensor 
##########################

<% @sensor_vars.each do |var, enabled| %>
	<% if enabled %>
<%= "#{var}" %>
	<% else %>
<%= "\##{var}" %>
	<% end %>
<% end %>
